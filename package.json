{
  "name": "redux-effects-credentials",
  "description": "Add credentials to fetch requests when conditions are met",
  "repository": "git://github.com/redux-effects/redux-effects-credentials.git",
  "version": "0.1.10",
  "license": "MIT",
  "main": "lib/index.js",
  "scripts": {
    "prepublish": "rm -rf lib && babel src --out-dir lib",
    "postpublish": "rm -rf lib"
  },
  "dependencies": {},
  "devDependencies": {
    "babel-preset-stage-2": "^6.3.13",
    "babel-preset-es2015": "^6.3.13",
    "babel-tape-runner": "^2.0.0",
    "tape": "^4.2.0"
  }
}
